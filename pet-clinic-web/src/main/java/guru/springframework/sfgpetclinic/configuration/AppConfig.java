package guru.springframework.sfgpetclinic.configuration;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {

    /*@Bean
    public OwnerService ownerService(OwnerRepository ownerRepository, PetRepository petRepository, PetTypeRepository petTypeRepository) {
        return new OwnerSDJpaService(ownerRepository, petRepository, petTypeRepository);
    }*/

    /*@Bean
    public VetService vetService(VetRepository vetRepository) {
        return new VetSDJpaService(vetRepository);
    }*/

    /*@Bean
    public VisitService visitService(VisitRepository visitRepository) {
        return new VisitSDJpaService(visitRepository);
    }*/
}
